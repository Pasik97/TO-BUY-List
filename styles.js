import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        paddingTop: 20,
        display: 'flex',
        flex: 1,
        backgroundColor: '#1a1a1a',
        color: 'white',
        position: "relative",
    },
});
